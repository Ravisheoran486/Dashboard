import React from 'react'
import * as Mock from '@visx/mock-data';

function Data() {
  const stock = Mock.appleStock;
  const points = Mock.genRandomNormalPoints();
  const bitcoin = Mock.browserUsage;
 // console.log(bitcoin);
  return (
    <div>Data</div>
  )
}

export default Data